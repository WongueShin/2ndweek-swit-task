export interface MockDataType {
  userId: string;
  userName: string;
  profileImage: string;
  content: string;
  date: string;
}

export interface UserIdType {
  userId: string,
  loginUser: string,
}
